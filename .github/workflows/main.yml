name: GPU-RDP  # Changed the name for you

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    # NOTE: 'windows-latest' HAS NO GPU. 
    # Change this to 'self-hosted' if you connect your own GPU VM.
    runs-on: windows-latest 
    timeout-minutes: 3600

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
          # Enable GPU Acceleration for RDP
          Write-Host "Enabling Hardware Acceleration for RDP..."
          $registryPath = "HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services"
          if (!(Test-Path $registryPath)) { New-Item -Path $registryPath -Force }
          Set-ItemProperty -Path $registryPath -Name "fEnableHardwareGraphicsAcceleration" -Value 1
          
          Restart-Service -Name TermService -Force

      - name: Create RDP User
        run: |
          $password = "G3mplayz!"
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP_GPU" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP_GPU"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP_GPU"

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-gpu-runner
          $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Maintain Connection
        run: |
          Write-Host "=== GPU RDP ACTIVE ==="
          Write-Host "IP: $env:TAILSCALE_IP"
          Write-Host "User: RDP_GPU"
          while ($true) { Start-Sleep -Seconds 300 }
